backend:
  name: git-gateway
  branch: NissimDev # Branch to update (optional; defaults to master)

media_folder: static/assets/uploads
public_folder: /assets/uploads

collections:
  - name: "blog"                                      # Used in routes, e.g., /admin/collections/blog
    label: "Blog"                                     # Used in the UI
    folder: "content/blog"                            # The path to the folder where the documents are stored
    create: true                                      # Allow users to create new documents in this collection
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"       # Filename template, e.g., YYYY-MM-DD-title.md
    fields:                                           # The fields for each document, usually in front matter
      - {label: "Layout", name: "layout", widget: "hidden", default: "blog"}
      # - {label: "Name of the File", name: "title", widget: "string"}
      # - {label: "Title of the Blog", name: "blogTitle", widget: "string"}
      - {label: "Title", name: "title", widget: "string"}
      - {label: "Publish Date", name: "date", widget: "datetime"}
      - {label: "Featured Image", name: "thumbnail", widget: "image"}
      - {label: "Rating (scale of 1-5)", name: "rating", widget: "number"}
      - {label: "Body", name: "body", widget: "markdown"}


### use TITLE field as file-name. Add new field for blog title to use in-blog